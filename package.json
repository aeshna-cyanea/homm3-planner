{
  "name": "hota-production-planner",
  "private": true,
  "scripts": {
    "build:standalone": "node build_standalone.js",
    "pretest:visual": "npm run build:standalone",
    "test:visual": "playwright test",
    "pretest:visual:update": "npm run build:standalone",
    "test:visual:update": "playwright test --update-snapshots",
    "test:visual:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  },
  "dependencies": {
    "tom-select": "2.6.2"
  }
}
